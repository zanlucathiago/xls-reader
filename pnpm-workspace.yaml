# esbuild's postinstall links its prebuilt platform binary; approve it so pnpm
# runs the build instead of erroring on an "ignored build" in CI (non-TTY).
allowBuilds:
  esbuild: true
# Skip pnpm's pre-script "are deps in sync" check (CI installs explicitly).
verifyDepsBeforeRun: false
